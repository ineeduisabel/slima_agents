# Slima API Token（選填 — 未設定時會自動讀取 ~/.slima/credentials.json）
SLIMA_API_TOKEN=

# Slima API 基底 URL（選填 — 預設為 https://api.slima.ai）
SLIMA_BASE_URL=https://api.slima.ai

# Agent 預設模型（選填）
SLIMA_AGENTS_MODEL=claude-sonnet-4-20250514
